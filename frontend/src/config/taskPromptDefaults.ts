/**
 * 与后端 `app/services/system_settings.py` 中 HOURLY_DIGEST_DEFAULT_PROMPT 保持一致。
 * 当 GET 未带 prompt 时用于表单展示兜底。
 */
export const HOURLY_DIGEST_DEFAULT_PROMPT = `【选稿】从过去一小时内已入库的候选条目中，挑出最值得写进本小时综述的稿件。优先：对用户决策或认知有明显信息增益、重大突发与时效强、存在多源互证的热点。排除：纯标题党、重复灌水、几乎没有有效信息的条目。

【综述】写成中文「私人秘书式」每小时资讯综述：语气克制、信息密度高，像在向用户口头汇报这一小时最值得关心的动态。不要用 1.2.3. 清单体或连续多个小节标题堆叠；用 2～5 个自然段连贯叙述，段与段之间空一行。在叙述中自然穿插素材里给出的 Markdown 本地链接（形式为 [可见文案](/reader/内容ID)），至少引用超过半数的入选素材；链接必须原样复制素材中的路径，禁止改用外站 http(s) 链接。不要编造素材中没有的信息。`
