export const PODCAST_SOURCES_ENABLED = false
export const KEYWORD_MONITORING_ENABLED = true

/** Score lab UI — only compiled into Vite dev builds (:3000). */
export const SCORE_LAB_BUILD_ENABLED = import.meta.env.DEV
