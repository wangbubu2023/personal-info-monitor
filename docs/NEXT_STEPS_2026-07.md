# 下一步建议（2026-07-07 晚，第二次复核）

> 唯一目标不变：**每天打开一个页面，用最少时间读完我关心的所有新信息（含付费墙 / X / YouTube），AI 帮我预消化。**
>
> 复核基准：commit `18d0572` + 当前**未提交**的 reader/digest 改动。与上午版相比，最大变化：**反思 4（阅读端无人区）正在被你亲手兑现**——未提交改动里已有 j/k 上下篇、r 已读、l 稍后读、按事件分组折叠、readerFlow 序列。这改变了优先级排序。

---

## 一、十条反思状态（本次实测）

| # | 反思 | 现状 | 判定 |
|---|------|------|------|
| 1 | 前半段功能冻结 | 今日提交基本是 reader（阅读侧）+ 安全加固 + 抓取修复，**没有新增抓取策略**——冻结事实上在被遵守。建议改写为「只修不增」 | **改写后保留** |
| 2 | 无真实质量数字 | 工具链齐（prelabel/review/export + eval_history + CI 回归门禁），但 `eval_set.jsonl` **仍是 4 条** | **仍有效**，只差跑一次 |
| 3 | 凭据寿命 = 系统寿命 | 代码层其实已支持持久化 profile（`user_data_dir` + storage_state 双通道），但 **MTTR 从未测过、过期演练从未做过**、weekly_report 无「距上次成功抓正文天数」 | **仍有效，头号优先级**，且是纯运营动作 |
| 4 | 阅读端是无人区 | 送达（daily_digest）✅ 结构化排版（content blocks + readerLayout）✅ 键盘流/稍后读/折叠 **正在未提交改动中** | **正在兑现**，收尾即可退休 |
| 5 | 反馈回看无日程 | scheduled task 列表为空，scheduler 无 8/5 任务 | **仍有效**，日期仍悬空 |
| 6 | 周体检邮件 | `weekly_report.py` 已建（周一 08:10） | **已退休** ✅ |
| 7 | 复杂度持续税 | vulture dead-code CI ✅；「增模块删等量」未成文 | 半有效 |
| 8 | SECURITY_MODEL.md | 不存在 | **仍有效** |
| 9 | 行数级预算 | 无 max_file_lines 门禁；四大文件原封不动（pimctl 1702 / pim 1636 / website 1583 / CredentialsTab 955）。website 拆出了 helpers/parser 但主文件没瘦 | **仍有效** |
| 10 | 一次触碰规则 | CONTRIBUTING 无此条 | **仍有效** |

**一句话**：你今天的工作把反思 4 从「无人区」推到了「收尾阶段」，冻结也在事实上被遵守。现在整个系统只剩一个真正的结构性缺口——**反思 3（凭据寿命）一行代码都不缺，缺的是一次演练和三个运营动作**。其余全是「钉规矩」级别的小事。

---

## 二、TODO 清单

### P0 · 凭据寿命（唯一命门，全是运营动作，不用写新功能）

- [ ] **T1 过期演练测 MTTR**：手动清一个付费源 cookie → 等 `session_expired` 告警邮件 → capture-session 恢复 → 确认正文回来。记录总耗时，验收 ≤10 分钟。本周内做一次。
- [ ] **T2 启用持久化登录 profile**：代码已支持 `user_data_dir`，给每个付费源真人登录一次常驻 profile，确认 `browser_session_auth_ready` 为真、不再依赖序列化 cookie 注入。
- [ ] **T3 weekly_report 加一行**：每个付费源「距上次成功抓到正文」天数（当前报告没有此指标，源悄悄死掉无人知）。
- [ ] **T4 X 收敛到一条路**：自建 RSSHub（compose 文件已在 docs/）灌自己的 cookie，只走 rsshub；从 fallback 摘掉三个已死的 Nitter 实例；X 源砍到每天真读的账号。
- [ ] **T5 端到端验收 fixture**：每个付费源 + 每个 X 源一条「抓到真正文」的 acceptance 测试，红了当周必须修。

### P1 · 阅读动线收尾（正在做，别烂尾）

- [ ] **T6 提交当前未提交的改动**：9 个文件 + 2 个新 utils（readerFlow/readerLayout）。先跑前后端测试再提交。注意：`.git/index.lock` 有一个残留的 0 字节锁文件（今天 20:13），提交前先 `rm .git/index.lock`。
- [ ] **T7 补 hide（隐藏/不感兴趣）**：当前键位只有 j/k/r/l，反馈闭环需要 hide 信号入 lane 统计——没有负反馈，8/5 回看就少一半信息。
- [ ] **T8 排版验收**：随机抽 5 篇付费墙正文，标准是「不想跳回原站」。不达标继续调 readerLayout，不默认「能显示=完成」。

### P1 · eval 冷启动（工具全齐，只差 2 小时人肉）

- [ ] **T9**：`export_eval_candidates` 拉 200 条 → `prelabel_eval_candidates` LLM 预标 → 只仲裁分歧样本（约 2 小时）→ 替换 4 条玩具数据 → 跑出第一个真实 precision@20 / duplicate_rate。此后阈值、embedding、冻结解除才有依据。

### P2 · 钉规矩（每条 ≤半小时）

- [ ] **T10 反馈回看进日历**：建 2026-08-05 的 scheduled task，自动出三个数：事件总量、star/hide 的 lane 分布、star 内容的 final_score 分布（star 集中在低分 = 排序背离偏好）。
- [ ] **T11 `docs/SECURITY_MODEL.md`**：每个防护机制一行「防什么 / 不防什么 / 依据」。本轮 KDF、SSRF/rebinding 改动后尤其该写——写的过程就是威胁模型审查。
- [ ] **T12 CONTRIBUTING 补三条硬规矩**：① 每新增一模块删等量旧代码；② `max_file_lines` 门禁，基线 = 1702/1636/1583/955，只减不增；③ 一次触碰：因修 bug 打开四大文件时顺手拆出所改的那块。

---

## 三、执行顺序建议

**本周**：T6（半天，别让在手的活烂尾）→ T1+T2（一个下午）→ T9（2 小时）。
**下周**：T3/T4/T5、T7/T8。
**随手**：T10–T12。

如果只做一件事：**T1+T2**。阅读端你已经在做且方向对；付费墙 / X 抓不到的根子在登录态活不长，而修它不需要写代码，只需要一次演练加一次真人登录。
