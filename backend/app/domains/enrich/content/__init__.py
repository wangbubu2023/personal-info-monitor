"""Content-level enrich operations — summariser/translator orchestration.

Future cuts will land here:

* ``reprocess.py`` (Phase 4 step 4 — DONE; manual UI-triggered
  summary/translation regeneration).
* Summariser / Translator providers themselves (Phase 4 step 3, moving
  out of ``app.processors`` into ``platform.llm`` + thin enrich wrappers).
"""
